[phases.setup]
nixPkgs = ["php82", "php82Extensions.curl", "php82Extensions.pdo", "php82Extensions.pdo_pgsql"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "php -S 0.0.0.0:8080 -t ."
